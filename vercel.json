{
    "projects": [
      {
        "src": "server",    
        "buildCommand": "npm install", 
        "startCommand": "node index.js", 
        "routes": [
          { "src": "/api/(.*)", "dest": "/server/index.js" }
        ]
      }
    ]
  }
  